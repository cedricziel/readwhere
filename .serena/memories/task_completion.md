# Task Completion Checklist

Before completing a task:

1. **Run analysis**: `flutter analyze` or `dart analyze`
2. **Run tests**: `flutter test` (aim for 80%+ coverage)
3. **Format if needed**: `dart format .`
4. **Semantic commit**: Use appropriate prefix (`feat:`, `fix:`, `chore:`, `refactor:`)
