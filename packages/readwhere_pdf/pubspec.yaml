name: readwhere_pdf
description: PDF file handling, metadata extraction, and page rendering for ReadWhere.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  collection: ^1.18.0
  pdfrx: ^2.2.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
