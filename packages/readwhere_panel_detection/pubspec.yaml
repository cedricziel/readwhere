name: readwhere_panel_detection
description: Panel detection for comic book pages using connected component labeling.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  image: ^4.3.0

dev_dependencies:
  test: ^1.24.0
