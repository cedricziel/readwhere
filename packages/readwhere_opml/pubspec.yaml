name: readwhere_opml
description: OPML parsing and writing library for ReadWhere
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  xml: ^6.5.0
  equatable: ^2.0.5

dev_dependencies:
  test: ^1.25.0
